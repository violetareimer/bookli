export default {
    props: {
        id: String,
        cover: String,
        title: String,
        authors: Array,
        synopsis: String,
        year: Number,
        publisher: String,
        genres: String
    }
}
